# Datasource Backend Guide Moved

The canonical operator guide for flat-file, LDAP, PostgreSQL, MySQL, and
MariaDB signing datasources is now
[`datasource-backends.md`](datasource-backends.md). This compatibility page
preserves existing links; it does not define a second backend contract.
