# Publish the complete symbol result over Milter so scenario scripts can assert
# zero-score observations, such as the received delivery-status projection
# symbols, on the scanned message itself.
use = ["x-spamd-result"];
skip_local = false;
skip_authenticated = false;
