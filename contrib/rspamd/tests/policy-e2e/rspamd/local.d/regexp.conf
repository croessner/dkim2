POLICY_E2E_UNRELATED_REJECT {
  re = "/deterministic body for unrelated-reject/{body}";
  score = 100.0;
  description = "Deterministic unrelated rejection for the Policy E2E lane";
  group = "policy_e2e";
}
